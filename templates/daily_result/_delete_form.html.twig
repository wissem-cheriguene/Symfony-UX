<form method="post" action="{{ path('daily_result_delete', {'id': daily_result.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ daily_result.id) }}">
    <button class="btn">Delete</button>
</form>
